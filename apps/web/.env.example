# Authentication is server-only. Compose defaults NODE_ENV to development and generates
# API_SESSION_TOKEN at web startup only in development.
# Production must provide NODE_ENV=production and a non-empty external API_SESSION_TOKEN.