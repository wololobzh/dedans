# Graduation workflow

Graduation eligibility must be implemented as explicit, testable business rules in the domain/application layers.

Do not encode critical eligibility logic only in UI code, ad-hoc SQL, or external automation.
